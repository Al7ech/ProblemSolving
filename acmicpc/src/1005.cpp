#include <iostream>
#include <vector>

using namespace std;

struct structure
{
	int time;
	int top_num;
	vector<int> ables;
}

int main()
{
	int t;
	cin >> t;
	for(int tc=0;tc<t;tc++)
	{
		int n,k;
		int st[1000];
		cin >> n >> k;
		for(int i=0;i<n;i++) cin >> st[i].time;
		for(int i=0;i<k;i++)
		{
			int s,d;
			cin >> s >> d;

		}
	}
	return 0;
}
//unfinished
